[package]
name = "awp-types"
version = "0.1.0"
edition = "2021"
description = "Shared types for the Agentic Web Protocol (AWP)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
